# Use relative URL in dev so Vite proxies /api and /uploads to the backend (port 5000).
VITE_API_URL=/api/v1

# Set to "true" to use local mock data (no backend required)
# VITE_USE_MOCK_DATA=true

# Auto-logout after this many minutes of inactivity (default: 30)
# VITE_IDLE_TIMEOUT_MINUTES=30
